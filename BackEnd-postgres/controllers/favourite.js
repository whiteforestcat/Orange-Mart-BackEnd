const pool = require("../db/db")

// ADD TO FAVOURITES
const addToFavourites = async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error.message)
    }
}


module.exports = {}